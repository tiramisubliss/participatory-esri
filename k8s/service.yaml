
apiVersion: v1
kind: Service
metadata:
  name: participatory-planning
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: participatory-planning
  type: LoadBalancer
